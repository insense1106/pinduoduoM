<template>
  <div class="ad10">
    <h1>出现路由无法点击跳转的bug</h1>
    <hr />
    <p>
      this.$router.push('/home/shop') this.$router.push('/home/shop'+this.id)
      this.$router.push({name:'xxx'.path:'xxxx'})
      this.$router.push({name:'路由命名',params:{参数名:参数值,参数名:参数值}})
      this.$router.go(-1) this.$router.back() router.app router.mode
      router.currentRoute router.forward router.replace router.go
      router.beforeEach #router.beforeResolve router.afterEach $route.path
      返回字符串 $route.params 返回键值对 $route.query 返回键值对 $route.hash
      $route.fullPath $route.matched $route.name $route.redirectedFrom
      this.$router #vue根实例的属性 this.$route 只读属性 #路由信息对象
      router.beforeEach(to, from, next) router.beforeEach((to,from,next)=>{
      if(to.meta.requireAuth){ if(store.state.userId){ next() }else{
      next({path:'/b'}) } }else{ next() } }) 前置守卫： 全局的前置守卫：
      beforeEach beforeResolve 路由独享的守卫： beforeEnter 组件内的守卫：
      beforeRouterEnter、beforeRouterUpdate、beforeRouteLeave 后置守卫：
      全局的后置守卫： afterEachthis.$router.push('/home/shop')
      this.$router.push('/home/shop'+this.id)
      this.$router.push({name:'xxx'.path:'xxxx'})
      this.$router.push({name:'路由命名',params:{参数名:参数值,参数名:参数值}})
      this.$router.go(-1) this.$router.back() router.app router.mode
      router.currentRoute router.forward router.replace router.go
      router.beforeEach #router.beforeResolve router.afterEach $route.path
      返回字符串 $route.params 返回键值对 $route.query 返回键值对 $route.hash
      $route.fullPath $route.matched $route.name $route.redirectedFrom
      this.$router #vue根实例的属性 this.$route 只读属性 #路由信息对象
      router.beforeEach(to, from, next) router.beforeEach((to,from,next)=>{
      if(to.meta.requireAuth){ if(store.state.userId){ next() }else{
      next({path:'/b'}) } }else{ next() } }) 前置守卫： 全局的前置守卫：
      beforeEach beforeResolve 路由独享的守卫： beforeEnter 组件内的守卫：
      beforeRouterEnter、beforeRouterUpdate、beforeRouteLeave 后置守卫：
      全局的后置守卫： afterEachthis.$router.push('/home/shop')
      this.$router.push('/home/shop'+this.id)
      this.$router.push({name:'xxx'.path:'xxxx'})
      this.$router.push({name:'路由命名',params:{参数名:参数值,参数名:参数值}})
      this.$router.go(-1) this.$router.back() router.app router.mode
      router.currentRoute router.forward router.replace router.go
      router.beforeEach #router.beforeResolve router.afterEach $route.path
      返回字符串 $route.params 返回键值对 $route.query 返回键值对 $route.hash
      $route.fullPath $route.matched $route.name $route.redirectedFrom
      this.$router #vue根实例的属性 this.$route 只读属性 #路由信息对象
      router.beforeEach(to, from, next) router.beforeEach((to,from,next)=>{
      if(to.meta.requireAuth){ if(store.state.userId){ next() }else{
      next({path:'/b'}) } }else{ next() } }) 前置守卫： 全局的前置守卫：
      beforeEach beforeResolve 路由独享的守卫： beforeEnter 组件内的守卫：
      beforeRouterEnter、beforeRouterUpdate、beforeRouteLeave 后置守卫：
      全局的后置守卫： afterEachthis.$router.push('/home/shop')
      this.$router.push('/home/shop'+this.id)
      this.$router.push({name:'xxx'.path:'xxxx'})
      this.$router.push({name:'路由命名',params:{参数名:参数值,参数名:参数值}})
      this.$router.go(-1) this.$router.back() router.app router.mode
      router.currentRoute router.forward router.replace router.go
      router.beforeEach #router.beforeResolve router.afterEach $route.path
      返回字符串 $route.params 返回键值对 $route.query 返回键值对 $route.hash
      $route.fullPath $route.matched $route.name $route.redirectedFrom
      this.$router #vue根实例的属性 this.$route 只读属性 #路由信息对象
      router.beforeEach(to, from, next) router.beforeEach((to,from,next)=>{
      if(to.meta.requireAuth){ if(store.state.userId){ next() }else{
      next({path:'/b'}) } }else{ next() } }) 前置守卫： 全局的前置守卫：
      beforeEach beforeResolve 路由独享的守卫： beforeEnter 组件内的守卫：
      beforeRouterEnter、beforeRouterUpdate、beforeRouteLeave 后置守卫：
      全局的后置守卫： afterEachthis.$router.push('/home/shop')
      this.$router.push('/home/shop'+this.id)
      this.$router.push({name:'xxx'.path:'xxxx'})
      this.$router.push({name:'路由命名',params:{参数名:参数值,参数名:参数值}})
      this.$router.go(-1) this.$router.back() router.app router.mode
      router.currentRoute router.forward router.replace router.go
      router.beforeEach #router.beforeResolve router.afterEach $route.path
      返回字符串 $route.params 返回键值对 $route.query 返回键值对 $route.hash
      $route.fullPath $route.matched $route.name $route.redirectedFrom
      this.$router #vue根实例的属性 this.$route 只读属性 #路由信息对象
      router.beforeEach(to, from, next) router.beforeEach((to,from,next)=>{
      if(to.meta.requireAuth){ if(store.state.userId){ next() }else{
      next({path:'/b'}) } }else{ next() } }) 前置守卫： 全局的前置守卫：
      beforeEach beforeResolve 路由独享的守卫： beforeEnter 组件内的守卫：
      beforeRouterEnter、beforeRouterUpdate、beforeRouteLeave 后置守卫：
      全局的后置守卫： afterEachthis.$router.push('/home/shop')
      this.$router.push('/home/shop'+this.id)
      this.$router.push({name:'xxx'.path:'xxxx'})
      this.$router.push({name:'路由命名',params:{参数名:参数值,参数名:参数值}})
      this.$router.go(-1) this.$router.back() router.app router.mode
      router.currentRoute router.forward router.replace router.go
      router.beforeEach #router.beforeResolve router.afterEach $route.path
      返回字符串 $route.params 返回键值对 $route.query 返回键值对 $route.hash
      $route.fullPath $route.matched $route.name $route.redirectedFrom
      this.$router #vue根实例的属性 this.$route 只读属性 #路由信息对象
      router.beforeEach(to, from, next) router.beforeEach((to,from,next)=>{
      if(to.meta.requireAuth){ if(store.state.userId){ next() }else{
      next({path:'/b'}) } }else{ next() } }) 前置守卫： 全局的前置守卫：
      beforeEach beforeResolve 路由独享的守卫： beforeEnter 组件内的守卫：
      beforeRouterEnter、beforeRouterUpdate、beforeRouteLeave 后置守卫：
      全局的后置守卫： afterEachthis.$router.push('/home/shop')
      this.$router.push('/home/shop'+this.id)
      this.$router.push({name:'xxx'.path:'xxxx'})
      this.$router.push({name:'路由命名',params:{参数名:参数值,参数名:参数值}})
      this.$router.go(-1) this.$router.back() router.app router.mode
      router.currentRoute router.forward router.replace router.go
      router.beforeEach #router.beforeResolve router.afterEach $route.path
      返回字符串 $route.params 返回键值对 $route.query 返回键值对 $route.hash
      $route.fullPath $route.matched $route.name $route.redirectedFrom
      this.$router #vue根实例的属性 this.$route 只读属性 #路由信息对象
      router.beforeEach(to, from, next) router.beforeEach((to,from,next)=>{
      if(to.meta.requireAuth){ if(store.state.userId){ next() }else{
      next({path:'/b'}) } }else{ next() } }) 前置守卫： 全局的前置守卫：
      beforeEach beforeResolve 路由独享的守卫： beforeEnter 组件内的守卫：
      beforeRouterEnter、beforeRouterUpdate、beforeRouteLeave 后置守卫：
      全局的后置守卫： afterEachthis.$router.push('/home/shop')
      this.$router.push('/home/shop'+this.id)
      this.$router.push({name:'xxx'.path:'xxxx'})
      this.$router.push({name:'路由命名',params:{参数名:参数值,参数名:参数值}})
      this.$router.go(-1) this.$router.back() router.app router.mode
      router.currentRoute router.forward router.replace router.go
      router.beforeEach #router.beforeResolve router.afterEach $route.path
      返回字符串 $route.params 返回键值对 $route.query 返回键值对 $route.hash
      $route.fullPath $route.matched $route.name $route.redirectedFrom
      this.$router #vue根实例的属性 this.$route 只读属性 #路由信息对象
      router.beforeEach(to, from, next) router.beforeEach((to,from,next)=>{
      if(to.meta.requireAuth){ if(store.state.userId){ next() }else{
      next({path:'/b'}) } }else{ next() } }) 前置守卫： 全局的前置守卫：
      beforeEach beforeResolve 路由独享的守卫： beforeEnter 组件内的守卫：
      beforeRouterEnter、beforeRouterUpdate、beforeRouteLeave 后置守卫：
      全局的后置守卫： afterEachthis.$router.push('/home/shop')
      this.$router.push('/home/shop'+this.id)
      this.$router.push({name:'xxx'.path:'xxxx'})
      this.$router.push({name:'路由命名',params:{参数名:参数值,参数名:参数值}})
      this.$router.go(-1) this.$router.back() router.app router.mode
      router.currentRoute router.forward router.replace router.go
      router.beforeEach #router.beforeResolve router.afterEach $route.path
      返回字符串 $route.params 返回键值对 $route.query 返回键值对 $route.hash
      $route.fullPath $route.matched $route.name $route.redirectedFrom
      this.$router #vue根实例的属性 this.$route 只读属性 #路由信息对象
      router.beforeEach(to, from, next) router.beforeEach((to,from,next)=>{
      if(to.meta.requireAuth){ if(store.state.userId){ next() }else{
      next({path:'/b'}) } }else{ next() } }) 前置守卫： 全局的前置守卫：
      beforeEach beforeResolve 路由独享的守卫： beforeEnter 组件内的守卫：
      beforeRouterEnter、beforeRouterUpdate、beforeRouteLeave 后置守卫：
      全局的后置守卫： afterEachthis.$router.push('/home/shop')
      this.$router.push('/home/shop'+this.id)
      this.$router.push({name:'xxx'.path:'xxxx'})
      this.$router.push({name:'路由命名',params:{参数名:参数值,参数名:参数值}})
      this.$router.go(-1) this.$router.back() router.app router.mode
      router.currentRoute router.forward router.replace router.go
      router.beforeEach #router.beforeResolve router.afterEach $route.path
      返回字符串 $route.params 返回键值对 $route.query 返回键值对 $route.hash
      $route.fullPath $route.matched $route.name $route.redirectedFrom
      this.$router #vue根实例的属性 this.$route 只读属性 #路由信息对象
      router.beforeEach(to, from, next) router.beforeEach((to,from,next)=>{
      if(to.meta.requireAuth){ if(store.state.userId){ next() }else{
      next({path:'/b'}) } }else{ next() } }) 前置守卫： 全局的前置守卫：
      beforeEach beforeResolve 路由独享的守卫： beforeEnter 组件内的守卫：
      beforeRouterEnter、beforeRouterUpdate、beforeRouteLeave 后置守卫：
      全局的后置守卫： afterEachthis.$router.push('/home/shop')
      this.$router.push('/home/shop'+this.id)
      this.$router.push({name:'xxx'.path:'xxxx'})
      this.$router.push({name:'路由命名',params:{参数名:参数值,参数名:参数值}})
      this.$router.go(-1) this.$router.back() router.app router.mode
      router.currentRoute router.forward router.replace router.go
      router.beforeEach #router.beforeResolve router.afterEach $route.path
      返回字符串 $route.params 返回键值对 $route.query 返回键值对 $route.hash
      $route.fullPath $route.matched $route.name $route.redirectedFrom
      this.$router #vue根实例的属性 this.$route 只读属性 #路由信息对象
      router.beforeEach(to, from, next) router.beforeEach((to,from,next)=>{
      if(to.meta.requireAuth){ if(store.state.userId){ next() }else{
      next({path:'/b'}) } }else{ next() } }) 前置守卫： 全局的前置守卫：
      beforeEach beforeResolve 路由独享的守卫： beforeEnter 组件内的守卫：
      beforeRouterEnter、beforeRouterUpdate、beforeRouteLeave 后置守卫：
      全局的后置守卫： afterEachthis.$router.push('/home/shop')
      this.$router.push('/home/shop'+this.id)
      this.$router.push({name:'xxx'.path:'xxxx'})
      this.$router.push({name:'路由命名',params:{参数名:参数值,参数名:参数值}})
      this.$router.go(-1) this.$router.back() router.app router.mode
      router.currentRoute router.forward router.replace router.go
      router.beforeEach #router.beforeResolve router.afterEach $route.path
      返回字符串 $route.params 返回键值对 $route.query 返回键值对 $route.hash
      $route.fullPath $route.matched $route.name $route.redirectedFrom
      this.$router #vue根实例的属性 this.$route 只读属性 #路由信息对象
      router.beforeEach(to, from, next) router.beforeEach((to,from,next)=>{
      if(to.meta.requireAuth){ if(store.state.userId){ next() }else{
      next({path:'/b'}) } }else{ next() } }) 前置守卫： 全局的前置守卫：
      beforeEach beforeResolve 路由独享的守卫： beforeEnter 组件内的守卫：
      beforeRouterEnter、beforeRouterUpdate、beforeRouteLeave 后置守卫：
      全局的后置守卫： afterEach
    </p>
  </div>
</template>

<script>
export default {
  name: 'ad10',
  components: {},
  props: {},
  data() {
    return {};
  },
  computed: {},
  watch: {},
  created() {},
  mounted() {},
  methods: {}
};
</script>

<style lang="stylus" scoped>
.ad10
    background-image linear-gradient(to right top,#ffcc00 50%, #eee 50%)
    // background-size 100% calc(100%-30vh)
    background-repeat no-repeat
    &:after
        content ' '
        position fixed
        top 15px
        left 0
        right 0
        bottom 0
        background-color white
        opacity 0
        z-index 11
        overflow hidden
        // height 10px
    // &:before
    //     content ' '
    //     overflow hidden
    //     position fixed
    //     top 255px
    //     left 0
    //     right 0
    //     height 10px
    //     background-color white
    //     opacity 0.7
    //     z-index 11
</style>
